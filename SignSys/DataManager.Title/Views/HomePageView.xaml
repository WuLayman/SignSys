<UserControl x:Class="DataManager.Title.Views.HomePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataManager.Title.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <ImageBrush Stretch="Fill" ImageSource="/1.jpg"/>
        </Grid.Background>
        <TextBlock Text="{Binding Title}" FontSize="30" Foreground="Red" HorizontalAlignment="Center"  VerticalAlignment="Center" Style="{StaticResource MaterialDesignTitleTextBlock}"/>
    </Grid>
</UserControl>
